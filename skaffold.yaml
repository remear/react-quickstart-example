apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/remear/react-quickstart-example:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/remear/react-quickstart-example:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: react-quickstart-example
        chartPath: charts/react-quickstart-example
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/remear/react-quickstart-example"
          image.tag: "{{.TAG}}"
